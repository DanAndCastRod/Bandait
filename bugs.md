SOLO PUEDO MOVERME ENTRE BADAIT IDENTITY, HOST SESSION y JOIN SESSION no importa si soy leader o follower
Al dar start metronome no suena y solo se ve el parpadeo en el follower el leader no sabe si está ejecutando o no el metronomo, además cada vez que pare y de start el metronomo debe iniciar en el pulso 1.

No es posible navegar por el resto de la aplicacion