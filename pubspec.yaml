name: bandait
description: Bandait server application.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_web_socket: ^1.0.4
  web_socket_channel: ^2.4.0
  uuid: ^4.2.1
  logging: ^1.2.0
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.9
